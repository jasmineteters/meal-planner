import React from 'react';

import './fields-styles.css';

export const fields = ({ placeholder, handleChange }) => (
  <input
    className='fields'
    type='text'
    // placeholder={placeholder}
    // onChange={handleChange}
  />
);
