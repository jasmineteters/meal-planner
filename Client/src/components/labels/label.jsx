import React from 'react';

import './label-styles.css';

export const label = ({ placeholder, handleChange }) => (
  <input
    className='label'
    type='text'
    // placeholder={placeholder}
    // onChange={handleChange}
  />
);
